// JavaScript entry point for the future WedWise site.
// Keep this file dependency-free while the project remains a static prototype.

document.addEventListener("DOMContentLoaded", () => {
  document.documentElement.classList.add("js-enabled");
});
